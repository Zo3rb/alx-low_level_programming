#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: computes the absolute value of an integer.
 * @n: The number to check.
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
