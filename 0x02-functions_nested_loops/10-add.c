#include "main.h"

/**
 * add - Entry point
 *
 * Description: adds two integers and returns the result.
 * @a: the first number.
 * @b: the second number.
 *
 * Return: an Int (result).
 */
int add(int a, int b)
{
	return a + b;
}
